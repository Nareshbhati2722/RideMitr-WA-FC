# Base URL of the RideMitr WA MCP API (note the /api/mcp/v1 suffix).
RIDEMITR_WA_API_URL=https://crm.srv879786.hstgr.cloud/api/mcp/v1

# A bearer key minted in RideMitr WA → Admin Settings → MCP Tools (shown once).
RIDEMITR_WA_API_KEY=fck_live_PASTE_YOUR_KEY
